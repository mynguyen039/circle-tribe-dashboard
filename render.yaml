services:
  - type: web
    name: circle-tribe-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: NOTION_TOKEN
        sync: false
      - key: GEMINI_KEY
        sync: false
      - key: CHAT_DB_ID
        sync: false
